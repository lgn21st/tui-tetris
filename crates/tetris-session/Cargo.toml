[package]
name = "tetris-session"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
arrayvec.workspace = true
tetris-core.workspace = true

[dev-dependencies]
tetris-core = { path = "../tetris-core", features = ["test-support"] }
