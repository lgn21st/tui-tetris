[package]
name = "tetris-adapter-protocol"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
arrayvec = "0.7"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tetris-core = { path = "../tetris-core" }
