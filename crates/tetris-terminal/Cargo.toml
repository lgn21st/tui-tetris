[package]
name = "tetris-terminal"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
arrayvec = "0.7"
crossterm = { version = "0.28", features = ["use-dev-tty"] }
tetris-core = { path = "../tetris-core" }
